name: posts_api
description: An interface for an API to manage posts.
version: 0.0.1+1
publish_to: none

environment:
  sdk: ">=3.5.1 <4.0.0"

dependencies:
  equatable: ^2.0.7
  meta: ^1.15.0

dev_dependencies:
  test: ^1.25.8
  very_good_analysis: ^6.0.0
